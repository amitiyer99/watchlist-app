name: Breakout Scanner Refresh

# Manual / off-hours only — intraday refresh is handled by market-refresh.yml (every 10 min).

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: master-data-push
  cancel-in-progress: false

jobs:
  refresh-breakout:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1
    steps:
      - uses: actions/checkout@v7
        with:
          ref: master
          fetch-depth: 0

      - name: Sync to latest master
        run: bash scripts/ci-sync-master.sh

      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'

      - run: npm ci

      - run: bash scripts/market-refresh.sh
