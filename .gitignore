node_modules/
.session/
config.json
alert-log.json
debug/
debug-watchlist.png
test-links.js
find-urls.js
resolve-urls.js
explore-screener.js

# Backtest bar cache (regenerable, never commit)
.backtest-cache/

# Local run logs (never commit)
output.log
output_log.txt
pred_run.txt
prediction-*.txt

# ML dataset (large, regenerable)
dataset.jsonl
.dataset-cache/

# Stale lock files from interrupted generator runs (never commit)
*.lock

# Raw Screener.in CSV exports — personal, kept local. Only the parsed
# sidecar (docs/screenerin-tickers.json) is committed.
imports/
